---
description: Create production-grade frontend interfaces with high design quality
---

I need you to build a production-quality frontend component or page. Follow these rules:

1. **Read the design system first** — check globals.css for CSS variables (--green, --black, --deep, --surface, --panel), fonts (Exo 2, Chakra Petch, Rajdhani, Share Tech Mono), and existing component patterns.

2. **Design principles:**
   - Dark theme with green (#00e87b) accent
   - Clean spacing, no clutter
   - Responsive by default (mobile-first)
   - Subtle animations (hover states, transitions)
   - Accessible (proper contrast, focus states, aria labels)

3. **Code quality:**
   - TypeScript with proper types
   - Tailwind CSS using the design system variables
   - Server components by default, client only when needed
   - No unnecessary dependencies

4. **Before writing code:**
   - Show me the component hierarchy
   - List the props/state needed
   - Identify any data fetching requirements

What component or page do you want me to build?
