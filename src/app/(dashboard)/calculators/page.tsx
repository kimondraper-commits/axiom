import { CalculatorShell } from "@/components/calculators/calculator-shell";

export const metadata = { title: "Planning Calculators — AXIOM" };

export default function CalculatorsPage() {
  return <CalculatorShell />;
}
